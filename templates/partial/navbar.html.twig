{# {% extends 'base.html.twig' %}


{% block body %} #}

{# <div class="example-wrapper"> #}
    <nav class="navDesktop">
        {% for categorie in categories %}
            <a href="{{ path('app.shop' , {'category': categorie.id })}}" title="{{ categorie.name }}"> {{ categorie.name }} </a>
        {% endfor %}
    </nav>

   
{# </div> #}

<nav class="navMobile">
    <button class="navMenuBurger" id="navMenuBurger" title="Afficher le menu">
        <img src="{{ asset('img/burger48.svg') }}" alt="menu burger">
    </button>
</nav>

<nav class="navMobileSide" id="navMobileSide">
    <button class="navMobileClose" id ="navMobileClose" title="Fermer le menu">
        <img src="{{ asset('img/cancel48.svg') }}" alt="Fermer le menu">
    </button>
    {% for categorie in categories %}
        <a href="#" title="{{ categorie.name }}"> {{ categorie.name }} </a>
    {% endfor %}
</nav>
{# {% endblock %} #}
